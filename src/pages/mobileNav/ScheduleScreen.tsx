import { CalendarDays } from "lucide-react";

import StubScreen from "./StubScreen";

// Stub. Later: upcoming matches + lessons.
const ScheduleScreen = () => <StubScreen icon={CalendarDays} title="Schedule" />;

export default ScheduleScreen;
