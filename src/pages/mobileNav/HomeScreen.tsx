import { Home } from "lucide-react";

import StubScreen from "./StubScreen";

// Stub. Later: player landing dashboard (activity, next actions, etc.).
const HomeScreen = () => <StubScreen icon={Home} title="Home" />;

export default HomeScreen;
