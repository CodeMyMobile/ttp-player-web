VITE_API_URL=http://localhost:3000/api
VITE_GOOGLE_API_KEY=xxx
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
VITE_GOOGLE_CLIENT_ID=xxx
# Feature flags
# Enable the new multi-match Match Play creation flow (off by default).
VITE_MULTI_MATCH=false
# Enable the stub-driven "Log a result" flow (off by default; set true only in
# deploy previews — it renders fixture data, not real matches).
VITE_LOG_RESULT=false